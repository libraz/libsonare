#pragma once

#include "rt/processor_chain.h"

namespace sonare::mastering::common {
using ::sonare::rt::total_latency_samples;
using ::sonare::rt::total_latency_samples_q8;
}  // namespace sonare::mastering::common
