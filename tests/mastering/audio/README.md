# Mastering listening fixtures

Run `python3 tools/mastering_generate_listening_corpus.py` from the repository root to
generate deterministic WAV fixtures in this directory.

The generated dry files cover transient limiting, clipping repair, hum/noise
repair, and stereo/mix processing checks without depending on licensed audio.
