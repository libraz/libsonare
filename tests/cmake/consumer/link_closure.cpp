// Placeholder main for the per-archive link-closure executables. The archive
// under test is force-loaded whole by the link line, so this file deliberately
// calls nothing: the check is whether every object in that archive resolves
// against the target's declared link interface alone.
int main() { return 0; }
