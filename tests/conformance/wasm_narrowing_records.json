{
  "floor": {
    "files": 10,
    "containers": 411,
    "narrowings": 13,
    "readers": 29
  },
  "shapes": [
    {
      "name": "array_length",
      "receiver_pattern": "\\[\"length\"\\]$",
      "reason": "An array's own length, not a caller-supplied number. A JS array cannot be longer than the safe-integer range, so the read has nothing to saturate; the budget on that length is the array-length guard's, which the shared wasmArrayLikeLength applies."
    },
    {
      "name": "array_element",
      "receiver_pattern": "\\[[^\"\\]]+\\]$",
      "reason": "An element read from an array whose length was taken first, indexed by the loop counter. The value is a caller-supplied number like any other, but the receiver is the element rather than a named option, so it carries no field name a range error could report; the element's own domain is checked where the struct it fills is validated."
    }
  ],
  "narrowings": [
    {
      "file": "bindings/project/project_arrange.cpp",
      "receiver": "mode_val",
      "type": "int",
      "reason": "An enum ordinal range-checked against the warp-mode bounds before the cast is used, so a saturated value is refused by name."
    }
  ],
  "readers": [
    {
      "file": "bindings/analysis/quick.cpp",
      "symbol": "onsetWindowFrames",
      "reason": "Not a conversion: the narrowing is delegated whole to the shared intProperty, and what is local is the field's own domain. The shared reader guarantees the value survived the conversion unchanged, which a negative frame count does, and nothing downstream asks whether one means anything -- the peak picker returned an ordinary onset list for -1. The six onset window fields are the only place that domain applies, so asking it at the field keeps it off every other integer option."
    },
    {
      "file": "bindings/analysis/quick.cpp",
      "symbol": "seedFromVal",
      "reason": "A default seeder rather than an options-bag reader: it rejects a non-number, turns a non-positive value into a deterministic seed, and refuses anything past the type's maximum by name. Every value in the type's range is a legal seed, so truncating a fractional one cannot land outside the domain -- driving it, a seed moves the synthesized response, 4294967296 and Infinity are refused, and 0 / -1 / NaN all fall back to the documented default."
    },
    {
      "file": "bindings/effects/repair.cpp",
      "symbol": "repairFloatOption",
      "reason": "Delegates its narrowing to the shared checkedFloatFromVal rather than casting, so the conversion contract is single-sourced and only the default-and-absence handling is local. Kept out of the shared header for the same reason as its integer sibling: its options bag is positional in a way the generic reader is not. Not the same defect as the integer one and it had to be measured separately -- an overflow here produces inf rather than a plausible in-domain number, and the config validator refuses NaN but accepted inf, so four distinct absurd requests reached one identical result."
    },
    {
      "file": "bindings/effects/repair.cpp",
      "symbol": "repairIntOption",
      "reason": "Delegates its narrowing to the shared checkedIntFromVal rather than casting, so the conversion contract is single-sourced and only the default-and-absence handling is local. Kept out of the shared header because its options bag is positional in a way the generic reader is not."
    },
    {
      "file": "bindings/mixing/mixing.cpp",
      "symbol": "panModeOrdinalFromVal",
      "reason": "An enum resolver for the pan-law ordinal, taking the string and numeric spellings together. The numeric path requires a finite, integral value inside the enum's bounds before the cast, so a saturated, negative, fractional or non-finite ordinal is refused by name; the two legal ordinals pan differently, which is what makes those refusals a measurement rather than an unread field."
    },
    {
      "file": "bindings/project/project_bounce.cpp",
      "symbol": "integerField",
      "reason": "Reads through double rather than narrowing, so no integer cast happens here and the inline-narrowing population does not see it. It is a second numeric reader outside the shared family all the same. Its own range check is the mechanism: a value outside [0, max] or a fractional one is refused naming the field, and a non-finite one is refused by the shared requireNumberProperty it reads through."
    },
    {
      "file": "bindings/project/project_bounce.cpp",
      "symbol": "numberField",
      "reason": "The floating-point sibling of integerField, under the same standing. It type-checks and refuses a non-finite value through the shared requireNumberProperty and returns the double unnarrowed; the float narrowing is the caller's, and it can only overflow for values already far outside the field's physical domain, where the representable neighbours render identically."
    },
    {
      "file": "bindings/project/project_bounce.cpp",
      "symbol": "sampleDescLoopMode",
      "reason": "An enum resolver for the sample loop mode, accepting the SoundFont numeric value and the name spellings. The numeric path is integerField's, so the ordinal is refused outside [0, 3] and for a fractional or non-finite value; an unknown name is refused naming the accepted set."
    },
    {
      "file": "bindings/project/project_edit.cpp",
      "symbol": "automationCurveFromVal",
      "reason": "An enum resolver, not a size reader: the ordinal it returns is range-checked by the C ABI against the curve enum's bounds. Recorded here rather than folded into the shared family because the reader shape is incidental -- it exists to accept both the string and the numeric spelling of one enum."
    },
    {
      "file": "bindings/project/project_edit.cpp",
      "symbol": "automationTargetKindFromVal",
      "reason": "The sibling enum resolver for the automation target kind. Like automationCurveFromVal it exists to accept both spellings of one enum, and its numeric path requires a finite, integral ordinal inside the enum's bounds before the cast, so a saturated, negative or fractional one is refused rather than landing on a member."
    },
    {
      "file": "bindings/project/project_transcribe.cpp",
      "symbol": "signedField",
      "reason": "Not a conversion: the narrowing is delegated whole to the shared floatProperty, and what is local is the field's own sign domain, asked only when the key is present. Presence has to be what decides it -- 0 is the C ABI's 'use the library default' on all six of these fields, and an omitted key still has to arrive as 0, so a value check would either refuse omission or accept a written zero and answer with a default under a number the caller did not choose. Both directions measured: with each key omitted and again explicitly undefined or null, the note list is identical to the baseline, while a written 0 on referenceHz, minNoteMs or segmentationThresholdCents and a non-negative velocityFloorDb are refused with InvalidParameter."
    },
    {
      "file": "bindings/project/project_transcribe.cpp",
      "symbol": "velocityField",
      "reason": "signedField's standing, over the shared intProperty, for the one transcription field whose domain is a range rather than a sign. The same presence gate applies for the same reason, and 0 is additionally not a MIDI velocity. Measured: 0 and 128 are refused with InvalidParameter, omission reproduces the baseline exactly, and a fixedVelocity of 77 is observed on every emitted note-on, so the field is read rather than merely accepted. The message wording is the Node facade's verbatim, so one domain reports one way on both surfaces."
    }
  ],
  "positional": {
    "floor": {
      "registrations": 600,
      "declarations": 1300,
      "parameters": 24,
      "functions": 20,
      "files": 4
    },
    "shapes": [
      {
        "name": "sixty_four_bit",
        "types": [
          "int64_t",
          "uint64_t"
        ],
        "parameter_pattern": ".",
        "count": 27,
        "status": "benign",
        "defect": "refuses",
        "mechanism": [
          {
            "file": "bindings/wasm/dist/sonare.js",
            "built": true,
            "contains": "if(typeof value==\"number\"){value=BigInt(value)}",
            "note": "The shipped embind glue, where the 64-bit toWireType lives. It is the build's output and no committed file carries it, so the citation is marked built: an absent dist is not a failure, a dist whose glue no longer reads this way is. The other half of the claim -- that this population is the 64-bit one at all -- is derived from the declared types rather than cited."
          }
        ],
        "reason": "A 64-bit parameter is registered through __embind_register_bigint, not through the integer path: its toWireType converts a JS number with BigInt(value), which throws on a fractional and on a non-finite one, and no number below 2^63 has anything to wrap into. The blessing belongs to the width, not to the field: this pattern selects on the declared type alone, so the population is every 64-bit parameter -- timeline positions and page ordinals alike -- and naming it after one of those meanings would claim a check nothing performs. The one residue is a request past 2^63, where the BigInt-to-i64 conversion truncates; it is integral, so it cannot arrive by the fractional truncation these records exist for, and no field of any meaning here addresses a domain that large."
      }
    ],
    "constructors": [],
    "parameters": []
  },
  "float": {
    "floor": {
      "parameters": 7,
      "functions": 7,
      "files": 3
    },
    "distribution": [
      {
        "file": "bindings/features/core.cpp",
        "count": 5
      },
      {
        "file": "bindings/features/music.cpp",
        "count": 1
      },
      {
        "file": "bindings/features/spectrogram.cpp",
        "count": 1
      }
    ],
    "passthroughs": [
      {
        "js": "hzToMel",
        "symbol": "js_hz_to_mel",
        "index": 0,
        "name": "hz",
        "mechanism": [
          {
            "file": "bindings/node/src/feature_units.ts",
            "contains": "saturates to an infinity the same way the C conversion does"
          }
        ],
        "reason": "A total function over the whole real line, and documented as one on the surface a consumer reads. Refusing a saturated value here would make the mel mapping disagree with the C ABI, with Node and with the reference implementation it mirrors, for a conversion that has no rejecting branch to reach."
      },
      {
        "js": "melToHz",
        "symbol": "js_mel_to_hz",
        "index": 0,
        "name": "mel",
        "mechanism": [
          {
            "file": "bindings/node/src/feature_units.ts",
            "contains": "Slaney mel scale to frequency in Hz. Total over the same domain as"
          }
        ],
        "reason": "The inverse of hzToMel under the same contract: the pair has to accept the same domain or a round trip stops being one."
      },
      {
        "js": "hzToMidi",
        "symbol": "js_hz_to_midi",
        "index": 0,
        "name": "hz",
        "mechanism": [
          {
            "file": "bindings/node/src/feature_units.ts",
            "contains": "whose unvoiced frames are NaN"
          }
        ],
        "reason": "Non-finite is meaningful input here rather than a caller error: an unvoiced frame of a pitch track is NaN, and mapping a whole track through this is the documented use. A checked reader refuses NaN, so routing it would reject the convention's own spelling."
      },
      {
        "js": "midiToHz",
        "symbol": "js_midi_to_hz",
        "index": 0,
        "name": "midi",
        "mechanism": [
          {
            "file": "bindings/node/src/feature_units.ts",
            "contains": "`-Infinity` bottoms out at 0 rather than propagating its sign"
          }
        ],
        "reason": "The inverse of hzToMidi, and its documented behaviour is specifically about what it does with an infinity -- a value it must therefore be able to receive."
      },
      {
        "js": "hzToNote",
        "symbol": "js_hz_to_note",
        "index": 0,
        "name": "hz",
        "mechanism": [
          {
            "file": "bindings/node/src/feature_units.ts",
            "contains": "past the representable MIDI range, and non-finite alike"
          }
        ],
        "reason": "Answers \"?\" for every frequency with no note, non-finite included, so a saturated value produces the same answer as any other unnameable one. The refusal a checked reader would add is a different contract, not a stricter one."
      },
      {
        "js": "vqt",
        "symbol": "js_vqt",
        "index": 6,
        "name": "gamma",
        "mechanism": [
          {
            "file": "include/sonare/sonare_c_features.h",
            "contains": "Negative or NaN selects the"
          },
          {
            "file": "src/feature/vqt.cpp",
            "contains": "config.gamma < 0.0f || std::isnan(config.gamma)"
          }
        ],
        "reason": "A NaN gamma is one of the two spellings of the automatic ERB-derived bandwidth, alongside a negative value, and the core resolves both. A checked read would refuse the sentinel rather than an overflow. The wrapper still refuses an infinity, which selects nothing."
      },
      {
        "js": "vqtToAudio",
        "symbol": "js_vqt_to_audio",
        "index": 7,
        "name": "gamma",
        "mechanism": [
          {
            "file": "include/sonare/sonare_c_features.h",
            "contains": "Negative or NaN selects the"
          },
          {
            "file": "src/feature/vqt.cpp",
            "contains": "config.gamma < 0.0f || std::isnan(config.gamma)"
          }
        ],
        "reason": "The inverse of vqt under the same gamma contract: the pair has to accept the same sentinel or a round trip through the automatic bandwidth cannot be spelled on this surface. An infinity is still refused."
      }
    ]
  }
}
