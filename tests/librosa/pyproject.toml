[project]
name = "libsonare-librosa-reference"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "librosa==0.11.0",
  "numpy>=2.0",
  "scipy>=1.14",
  "wheel>=0.40",
]

[tool.rye]
managed = true
virtual = true
