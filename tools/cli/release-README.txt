libsonare native CLI

This archive contains the standalone sonare-cli binary for the selected platform.

Supported input formats: WAV and MP3. These release assets are built with FFmpeg disabled, so M4A, AAC, FLAC, OGG, Opus, and other FFmpeg-backed formats are not available.

The Python package installs a different command named sonare. Both commands can coexist on PATH.
